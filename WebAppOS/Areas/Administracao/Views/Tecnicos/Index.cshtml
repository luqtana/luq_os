@model IEnumerable<WebAppOS.Models.Tecnico>

@{
    ViewBag.Title = "Lista de Técnicos";
}

<p>
    @Html.ActionLink("Cadastar novo técnico", "Create")
</p>
<h2>Lista de Técnicos Cadastrados</h2>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CPF)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CPF)
        </td>
        <td>
            @Html.ActionLink("Atualizar", "Edit", new { id=item.TecnicoId }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.TecnicoId }) |
            @Html.ActionLink("Apagar", "Delete", new { id=item.TecnicoId })
        </td>
    </tr>
}

</table>
